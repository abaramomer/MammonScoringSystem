@model ScoringSystem.Models.VariableQuestionModel

<div class="variable-question" id="@Model.Id">
    <select>
        @foreach (var answer in Model.Answers)
        {
            <option value="@answer">@answer.Text</option>
        }
    </select>
</div>